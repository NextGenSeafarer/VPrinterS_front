
export const Loader = ({className}) => {
    return (

            <div className={`loader ${className}`}>
                <div className={'loader__body'}>
                </div>
            </div>

    )
}